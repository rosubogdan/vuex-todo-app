import { API_URL } from './api';
import { ROUTES, LOGGED_IN_ROUTES, LOGGED_OUT_ROUTES, CONTENT_ROUTES } from './routes';
import { CONTENT } from './content/content';
import { TABLE_FIELDS } from './content/table';

const STORE_AUTH_MODULE = 'AUTH';
const STORE_TODOS_MODULE = 'TODOS';

export {
  API_URL,
  ROUTES,
  LOGGED_IN_ROUTES,
  LOGGED_OUT_ROUTES,
  CONTENT_ROUTES,
  CONTENT,
  TABLE_FIELDS,
  STORE_AUTH_MODULE,
  STORE_TODOS_MODULE
};
